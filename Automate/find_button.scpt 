-- 坐标调试脚本
tell application "100 LG" to activate
delay 2

display dialog "请完成以下步骤：
1. 将鼠标移动到第一个按钮上
2. 不要移动鼠标
3. 点击确定" buttons {"确定"} default button 1

-- 获取鼠标位置（多种方法）
-- set mousePos1 to do shell script "python3 -c 'import pyautogui; pos = pyautogui.position(); print(f\"{pos[0]},{pos[1]}\")'"
set mousePos2 to do shell script "/opt/homebrew/Cellar/cliclick/5.1/bin/cliclick p"

display dialog "实际按钮坐标：
-- Python: " & mousePos1 & "
cliclick: " & mousePos2 & "

请记录这些坐标用于脚本中" buttons {"确定"} default button 1
